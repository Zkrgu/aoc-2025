Parse = reshape infinity_2 parse regex "\\d+" &rs infinity 0
Range = /(content join)+row(box range+1) off - uncouple transpose
P,1 = /+ * on(= dip mod below(floor div) by(exp,10 div 2 ceil log,10))
P,2 = /+rows(parse first transpose regex "(\\d+)\\1+") unparse
P,1 Range Parse
